import { createTheme } from "@mui/system";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#B73C58'
        },
        secondary: {
            main: '#7c2f4b'
        },
        background: {
            paper: '#fff',
        },
        
    },
})